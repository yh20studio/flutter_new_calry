name: flutter_new_calry
description: A new Flutter project.


publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  http: ^0.13.3
  url_launcher: ^6.0.5
  shared_preferences: ^2.0.5
  flutter_neumorphic: ^3.2.0
  

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

